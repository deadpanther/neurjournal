# NeuroJournal Backend Dependencies
fastapi>=0.115.0
uvicorn>=0.30.0
httpx>=0.27.0
pydantic>=2.0
numpy>=2.0

# PyTorch (MPS for Apple Silicon)
torch>=2.5.1
torchvision>=0.20

# TRIBE v2 is installed from source via setup.sh
# It brings: transformers, einops, gtts, langdetect, spacy, etc.

# HuggingFace (for model download)
huggingface_hub

# Semantic search (replaces TF-IDF)
sentence-transformers>=3.0

# Emotion detection
transformers>=4.40

# Memory search fallback
scikit-learn>=1.5
